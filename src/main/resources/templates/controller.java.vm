package ${controllerPath};

import java.util.Arrays;
import java.util.Map;
import com.cszl.base.BaseContrller;
import org.springframework.web.bind.annotation.*;
import java.io.Serializable;
import com.baomidou.mybatisplus.extension.plugins.pagination.Page;
import io.swagger.annotations.Api;
import io.swagger.annotations.ApiOperation;
import lombok.extern.slf4j.Slf4j;

import ${entityFullName};
import ${serviceFullName};
import ${requestFullName};
import com.cszl.base.response.Result;



/**
 * ${tableComment}控制层
 *
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
@Slf4j
@Api(tags = "${tableComment}相关接口")
@RestController
@RequestMapping("${module}")
public class ${controllerName} extends BaseController<${serviceName}>{

    ${methods}

}
